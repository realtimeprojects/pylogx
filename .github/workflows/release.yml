name: release

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: releases/**

jobs:
  publish-pylox:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
    - uses: actions/checkout@v3
      with:
        path: pylogx

    - name: build pylogx
      run: |
        echo building pylogx

    - name: publish pylogx
      run: |
        echo publishing pylogx
